Run the following commands after changing the current
directory to the project root.

To build the program run:
    make

To clean .o files run:
    make clean

To clean .o files and exe run:
    make cleanall

To run the program run:
    ./main